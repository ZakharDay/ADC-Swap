<div style="display: flex; justify-content: space-between; width: 70%; margin: 0 auto
">
<div>
  <%= link_to("Main", welcome_index_path) %>
</div>
<div>
  <% if user_signed_in? %>
  <%= link_to("My requests", profile_exchange_requests_path(current_user.id)) %>
  <%= link_to("My profile", profile_path(current_user.id)) %>

  <% else %>

  <%= link_to('Login', new_user_session_path) %>
  <%= link_to('Sign Up', new_user_registration_path) %>

  <% end %>

</div>

</div>
