<div style="display: flex; justify-content: space-between; width: 70%; margin: 0 auto
">
<div>
  <%= link_to("Main", welcome_index_path) %>
</div>
<div>
  <% if user_signed_in? %>
    <%= link_to("My requests", exchange_requests_path) %>
    <%= link_to("My profile", profile_path(current_user.profile.id)) %>

  <% else %>

  <%#= render 'button' %>

  <%= link_to('Sign Up', new_user_registration_path) %>

  <% end %>

</div>

</div>
